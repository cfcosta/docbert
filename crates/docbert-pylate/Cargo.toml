[package]
name = "docbert-pylate"
version = "0.10.0"
edition = "2021"
license = "MIT"
description = "Rust library for late interaction (ColBERT) models, vendored into the docbert workspace."

[dependencies]
candle-core = "0.11.0"
candle-nn = "0.11.0"
candle-transformers = "0.11.0"
candle-flash-attn = { version = "0.11.0", optional = true }
tokenizers = { version = "0.23", default-features = false, features = ["onig"] }
serde_json = "1.0.150"
serde = { version = "1.0.228", features = ["derive"] }
thiserror = "2.0.18"
hf-hub = { version = "0.5.0", default-features = false, features = [
  "ureq",
  "rustls-tls",
] }
kodama = "0.3.0"
rayon = "1.12.0"

[features]
default = []

metal = ["candle-core/metal", "candle-nn/metal", "candle-transformers/metal"]
cuda = [
  "candle-core/cuda",
  "candle-nn/cuda",
  "candle-transformers/cuda",
  "dep:candle-flash-attn",
]
mkl = ["candle-core/mkl", "candle-nn/mkl", "candle-transformers/mkl"]
accelerate = [
  "candle-core/accelerate",
  "candle-nn/accelerate",
  "candle-transformers/accelerate",
]

[dev-dependencies]
anyhow = "1.0.103"
criterion = { version = "0.8", features = ["html_reports"] }
hegeltest = "0.28"

[[bench]]
name = "encode_batch_size"
harness = false

# Compares the eager masked forward against the CUDA-only flash
# paths, so it cannot build without them.
[[example]]
name = "attention_parity"
required-features = ["cuda"]
